// first i will import the java scanner
import java.util.scanner;

public class MyClass {
   public static void main(string[] orgs) {
   
   // i will create instance which takes the imput
   //from the user
Scanner reader = new Scanner(System.in);
System.out.printIn("please enter a number");

// i will put the integer into a variable
int number = reader.nextInt();

//i will print it
System.out.printIn("the number is" + number);
 
 
  }
}
